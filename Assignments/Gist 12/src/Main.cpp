/*Gist 12: write a function that multiplies two numbers. 
You can only use addition or subtraction and you can only 
check whether a number is positive negative or 0. Can not 
use > >= < <=

Return and parameters are unsigned integers

Uint mult(uint x1, uint x2){}*/

#include <iostream>

int main()
{
	system("pause");
	return 0;
}